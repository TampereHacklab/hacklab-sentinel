extend layout

block content
    div(class="container-fluid")
        table(class="table table-striped")
            thead
                tr
                    th username
                    th e-mail
                    th last seen
                    th created
            tbody
                each user in users
                    tr
                        td= user.username
                        td= user.email
                        td= user.lastlogin
                        td= user.created
